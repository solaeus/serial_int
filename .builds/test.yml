image: ubuntu/latest
packages:
  - curl
sources:
  - https://git.sr.ht/~jeffa/serial_int
tasks:
  - setup: |
      curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
  - run: |
      cd serial_int
      sh test.sh
